allprojects {
    group 'cn.codethink'
    version '1.0.0-SNAPSHOT'
}

ext {
    junitVersion = '5.9.2'
    expressionVersion = '1.1.0-SNAPSHOT'
    publishScriptFile = new File(project.rootDir, 'publish.gradle')
}
allprojects {
    group 'cn.codethink'
    version '1.0.0-SNAPSHOT'
}

ext {
    junitVersion = '5.9.2'
    expressionVersion = '1.0.0-SNAPSHOT'
    guavaVersion = '31.1-jre'

    publishScriptFile = new File(project.rootDir, 'publish.gradle')
}